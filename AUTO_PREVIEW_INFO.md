# 🎯 Превью проектов

## ⚠️ Автоматические скриншоты отключены

**Причина:** Бесплатные сервисы скриншотов (S-Shot.ru и др.) блокируют запросы.

**Решение:** Используются статичные изображения из `/public`

---

## 🚀 Как это работает

### Компонент `AutoPreview`

Создан специальный React компонент, который:

1. **Получает URL** сайта проекта
2. **Автоматически создает** скриншот через Screenshot API
3. **Показывает превью** на карточке проекта
4. **Fallback** - если API не работает, показывает статичное изображение

---

## 📁 Файлы

### `components/auto-preview.tsx`
```typescript
<AutoPreview
  url={project.url}
  fallbackImage={project.image}
  alt={project.title}
  className="h-full w-full object-cover"
/>
```

**Параметры:**
- `url` - URL сайта для скриншота
- `fallbackImage` - резервное изображение
- `alt` - alt текст
- `className` - CSS классы

---

## 🔧 Используемый API

### Screenshot Machine (Demo)

**URL формат:**
```
https://api.screenshotmachine.com/?key=demo&url=[URL]&dimension=1200x675
```

**Особенности:**
- ✅ Бесплатный demo ключ
- ✅ Без регистрации
- ✅ Автоматическое создание скриншотов
- ⚠️ Ограничения demo ключа

---

## 📊 Где используется

### 1. Страница проектов (`/projects`)
- Карточки проектов
- Автоматические превью
- Размер: 1200x675px

### 2. Детальная страница (`/projects/[id]`)
- Большое превью
- Автоматический скриншот
- Fallback на статичное изображение

---

## 🎯 Преимущества

### ✅ Автоматизация:
- Не нужно делать скриншоты вручную
- Просто добавьте URL проекта
- Превью создастся автоматически

### ✅ Актуальность:
- Скриншоты обновляются
- Отражают текущее состояние сайта
- Не нужно обновлять вручную

### ✅ Надежность:
- Fallback система
- Если API не работает - показывается статичное изображение
- Всегда что-то отображается

---

## 🔄 Fallback система

```typescript
onError={(e) => {
  // Если автопревью не загрузилось
  const target = e.target as HTMLImageElement
  if (target.src !== fallbackImage) {
    target.src = fallbackImage // Показываем резервное изображение
  }
}}
```

**Порядок попыток:**
1. Screenshot API → автоматический скриншот
2. Fallback → статичное изображение из `/public`
3. Placeholder → `/placeholder.svg`

---

## 🎨 Как добавить новый проект

Просто добавьте в `lib/projects-data.ts`:

```typescript
{
  id: "my-project",
  title: { en: "My Project", uk: "Мій проєкт", de: "Mein Projekt" },
  url: "https://my-site.com", // ← Превью создастся автоматически!
  image: "/fallback-image.jpg", // ← Опционально: резервное изображение
  // ... остальные поля
}
```

**Готово!** Превью появится автоматически.

---

## 🔧 Альтернативные API

Если Screenshot Machine не работает, можно заменить в `components/auto-preview.tsx`:

### ApiFlash:
```typescript
return `https://api.apiflash.com/v1/urltoimage?access_key=demo&url=${encodedUrl}&width=1200&height=675`
```

### PagePeeker:
```typescript
return `https://api.pagepeeker.com/v2/thumbs.php?size=l&url=${url}`
```

### Microlink:
```typescript
return `https://api.microlink.io/?url=${encodedUrl}&screenshot=true&meta=false&embed=screenshot.url`
```

---

## ⚙️ Настройка размера

Измените в `components/auto-preview.tsx`:

```typescript
// Для карточек (маленькие)
dimension=800x450

// Для списка (средние)
dimension=1200x675

// Для детальной страницы (большие)
dimension=1920x1080
```

---

## 🐛 Решение проблем

### Превью не загружается:

**Причина:** API недоступен или лимит исчерпан

**Решение:**
1. Проверьте консоль браузера (F12)
2. Система автоматически покажет fallback изображение
3. Можно заменить API на другой

### Превью устаревшее:

**Причина:** Кеширование

**Решение:**
- Обновите страницу (Ctrl+F5)
- Добавьте `&fresh=true` к URL API
- Подождите обновления кеша

### Хочу использовать свои изображения:

**Решение:**
Замените в коде:

```typescript
// Вместо AutoPreview используйте обычный img
<img src={project.image} alt={alt} className={className} />
```

---

## 📊 Производительность

### Оптимизация:

1. **Lazy loading:**
```typescript
loading="lazy"
```
Изображения загружаются только при скролле

2. **Fallback:**
Если API медленный - показывается резервное изображение

3. **Кеширование:**
Браузер кеширует скриншоты автоматически

---

## 💡 Рекомендации

### ✅ Лучшие практики:

1. **Всегда указывайте fallback:**
```typescript
fallbackImage={project.image || "/placeholder.svg"}
```

2. **Используйте HTTPS:**
```typescript
url: "https://site.com" // ✅ Хорошо
url: "http://site.com"  // ⚠️ Может не работать
```

3. **Проверяйте URL:**
Убедитесь, что сайт доступен публично

4. **Добавьте резервные изображения:**
На случай, если API не работает

---

## 🎯 Итог

### Что вы получили:

✅ **Автоматические превью** - создаются по URL  
✅ **Не нужно делать скриншоты** - всё автоматически  
✅ **Fallback система** - всегда что-то отображается  
✅ **Легко добавлять проекты** - просто URL!  
✅ **Актуальные скриншоты** - обновляются автоматически  

---

## 🚀 Готово!

Теперь просто добавляйте URL проекта - превью создастся автоматически!

**Проверьте:** http://localhost:3000/projects 🎉
